Spawn {{prompt_count}} {{subagent_name}} subagents in parallel, each with prompt: "Claim a prompt file by calling claim_prompt with step_type="{{step_name}}" and video_id="{{video_id}}". Your video_id is {{video_id}}."
