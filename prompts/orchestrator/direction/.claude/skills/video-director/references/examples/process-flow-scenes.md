# Process & Flow Scenes

## When to use
Read this file when the script describes step-by-step processes, "how X works" explanations, pipelines, sequential flows, circular tracks, node-to-node traversals, or any ordered sequence of stages.

<examples>

<example_1>
"audioTranscriptPortion": "So here's what happens in real time. A customer types in their card number. A payment gateway encrypts that data and fires it over to the acquirer. The acquirer passes it to the card network. The card network pings the issuing bank and basically asks, is this person good for it? The bank says yes or no, that answer bounces all the way back, and your checkout page shows accepted or declined.",
"videoDescription": "A large circular track is centered. Five nodes are placed at equal 72-degree intervals starting from 12 o'clock going clockwise: @customer at top center, @paymentgateway at upper right, @acquirerbank at lower right, @cardnetwork at lower left, @issuingbank at upper left. Each node is a card with three states: idle, active (slight scale-up), and completed. Labels sit radially outward from each card. A faint ring traces the full circular path. A circle with trailing circles behind it, each smaller than the previous, creating a chain, travels clockwise along the track, node-to-node. As it arrives at each node, a center panel appears showing a step illustration. On 'A customer types in their card number', at 'card number', it travels along the track to @customer and the center panel shows a Stripe checkout UI mockup (~45% canvas height) inside an iPhone: a 'Payment details' header, a card number input with @cardbrandicon showing '4532 8721 0093' typing progressively character by character with a blinking cursor, a split row below with 'MM / YY' and 'CVC' fields, and a 'PAY $129.99' button. On 'A payment gateway encrypts that data', at 'encrypts', it travels along the track to @paymentgateway and the center panel shows three concentric rings spinning in opposite directions around a central @padlockicon. On 'and fires it over to the acquirer', at 'the acquirer', it travels along the track to @acquirerbank and the center panel shows two rectangles (source document and destination) with an animated arrow sliding left-to-right between them. On 'The card network pings the issuing bank', at 'pings', it travels along the track to @cardnetwork and the center panel shows a sonar/radar animation with four expanding concentric rings radiating outward from a center circle, creating a 'ping' effect. On 'is this person good for it', at 'good for it', it travels along the track to @issuingbank and the center panel shows a large circle with a question mark that transitions to a @checkmarkbadge when funds are verified, title changing from 'VERIFYING FUNDS' to 'APPROVED'. On 'that answer bounces all the way back', at 'bounces', the chain reverses direction, traveling counter-clockwise along the track back through all nodes. A center label shows 'RESPONSE / bouncing back through the chain'. After return completes, a full-circle success ring appears."
</example_1>

<example_2>
"audioTranscriptPortion": "Billions of times per second. Fetch. Decode. Execute. Write back.",
"videoDescription": "A massive CPU chip fills the dead center of the frame, taking up a significant portion of the canvas. The chip is drawn entirely in SVG -- do NOT use the @cpu asset image. Draw it as a large rounded-corner square with a fill and a subtle border. Along each edge, short evenly-spaced pin stubs extend outward as simple lines -- clean and geometric. There is no label or text on the chip face -- the register bars and ALU chevron fill the chip interior directly.\n\nELLIPTICAL ORBIT: Surrounding the chip, a visible elliptical orbit path is drawn -- a large horizontal ellipse (wider than it is tall) centered on the chip -- the ellipse must be significantly larger than the chip so there is clear empty space between the chip edges and the orbit ring on all sides. The ellipse is rendered as a clearly visible glowing ring with a subtle glow filter to make it feel like a light trail. The four pipeline stage labels are positioned at the four cardinal points of this ellipse:\n- 'FETCH' at the top\n- 'DECODE' on the right\n- 'EXECUTE' at the bottom\n- 'WRITE BACK' on the left\nEach label has a distinct accent color. Labels are bold, uppercase, in a clean monospace font. When inactive, the labels should still be clearly readable -- render them at reduced opacity. When active, the label jumps to full opacity and glows brightly in its accent color with a drop-shadow glow.\n\nTHE ORB: A glowing orb -- representing a single instruction -- travels along the elliptical orbit path, completing one full clockwise revolution over the course of the scene. The orb has a strong bloom glow and leaves a fading comet tail behind it as it moves. As the orb approaches each stage label, that label lights up brightly and the previous label dims back down. The orb movement should feel smooth and continuous, like a planet orbiting -- not stopping at each stage, just passing through.\n\nCHIP INTERIOR (subtle, secondary): Inside the chip surface, two elements react to the pipeline as the orb passes each stage:\n- A simplified register file depicted as a vertical stack of six horizontal bars in the upper portion of the chip, each bar representing a register. When the orb reaches FETCH, the 2nd bar briefly glows. When it reaches DECODE, the 3rd bar briefly glows in DECODE's accent color. When it reaches WRITE BACK, the 5th bar briefly glows in WRITE BACK's accent color.\n- An ALU symbol -- a prominent V-shaped chevron -- sits below the registers at the center of the chip, with data lines feeding into it from above and a result line extending downward. When the orb reaches EXECUTE, the chevron briefly glows in EXECUTE's accent color.\n\nSTAGING: The chip fades in first with a spring scale animation. Then the four stage labels fade in one by one clockwise with staggered delays. Then the elliptical orbit path draws itself in. Finally the orb appears and begins its revolution.\n\nThe overall feel should be a clean animated diagram -- spacious, precise, with the glowing orb orbiting the chip as the visual centerpiece."
</example_2>

<example_3>
"audioTranscriptPortion": "Most teams will skip this. Someone has an idea, the team gets excited, and three weeks later you're building a game that dies the first time a real player touches it. You need to start with market analysis, references, brainstorming, but the key isn't coming up with one good idea. It's collecting several so you can compare them.",
"videoDescription": "A horizontal track line runs across the screen with three checkpoint gates labeled 'MARKET ANALYSIS', 'REFERENCES', 'BRAINSTORMING'. A dot accelerates along the track with reckless speed, a speed trail growing behind it. A '3 WEEKS' label trails the dot. The dot briefly illuminates each checkpoint gate as it blasts past without stopping. It slams into a wall barrier at the right edge -- the screen shakes on impact, crash fragments scatter, the track and fragments blur, and 'DEAD ON ARRIVAL' appears below the track line near the right edge at the impact point, not overlapping the line. On 'You need to start with market analysis, references, brainstorming', at 'start', the crashed track fades out and a new track draws in with the same three checkpoint gates. A 'PHASE 1 / PRE-PRODUCTION' label fades in centered. The track and gates blur behind it. After a brief hold, the label slides up to the top of the screen and stays there. The track unblurs, and a new dot enters from the left and moves slowly along the track. The dot follows the track line through three checkpoints. On 'market analysis', the dot reaches the MARKET ANALYSIS checkpoint exactly as the word is spoken, pauses briefly on it, and the checkpoint glows with expanding rings. On 'references', the dot reaches the REFERENCES checkpoint exactly as the word is spoken, pauses briefly on it, and the checkpoint glows. On 'brainstorming', the dot reaches the BRAINSTORMING checkpoint exactly as the word is spoken, pauses briefly on it, and the checkpoint glows. After BRAINSTORMING, on 'collecting several', the checkpoint gates fade out. The track line and its dot move upward to the top lane position -- the line becomes the first lane (CONCEPT 1). Four new horizontal lines slide downward from the first lane and arrange themselves below it so that all five lanes are equidistant. Each lane is a thin horizontal line spanning the full width of the screen. Each new dot starts at the same horizontal position as the first lane's dot, then all five dots move rightward together and stop at the right end of their lanes. Below each line on the left side, a label reads 'CONCEPT 1' through 'CONCEPT 5'. On 'fall in love with the first concept', all lanes except the top one (CONCEPT 1) dim. On 'never look at what else is on the table', all five lanes restore to full brightness."
</example_3>

</examples>
